import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'movie-finder-2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
